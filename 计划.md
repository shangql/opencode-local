# opencode 接入本地大模型功能开发计划

## 项目背景

- **本地大模型信息**：
  - Base URL: `http://192.168.102.99:8000/v1/chat/completions`
  - API Key: `sk-token1`
  - 模型类型: Qwen 系列模型
  - 模型数量: 20+ 个不同规格的 Qwen 模型

- **目标**：在 opencode 项目中增加接入本地大模型的功能，使其能够利用本地部署的 Qwen 模型进行编程辅助。

## 技术分析

### 现有架构优势

1. **内置 OpenAI 兼容 API 支持**：
   - opencode 已集成 `@ai-sdk/openai-compatible` 库
   - 支持自定义 baseURL 和 API Key
   - 已有 GitHub Copilot 等类似实现

2. **配置驱动架构**：
   - 支持通过 `opencode.jsonc` 配置文件定义 provider
   - 支持环境变量配置
   - 模块化的 provider 注册机制

3. **完善的模型管理**：
   - 支持模型列表查询 (`opencode models`)
   - 支持模型选择和切换
   - 统一的 API 调用接口

### 技术方案

**核心思路**：利用现有的 OpenAI 兼容 API 支持，创建自定义 provider 配置来接入本地大模型，无需大量修改源代码。

## 实施计划

### 阶段一：环境准备与分析

| 任务 | 描述 | 状态 |
|------|------|------|
| 1.1 | 验证本地大模型服务可用性 | ✅ 待执行 |
| 1.2 | 分析 opencode 项目结构 | ✅ 待执行 |
| 1.3 | 确认 OpenAI 兼容 API 支持 | ✅ 待执行 |

### 阶段二：核心实现

| 任务 | 描述 | 状态 |
|------|------|
| 2.1 | 创建本地模型 provider 配置 | ✅ 待执行 |
| 2.2 | 注册新的 provider 到系统 | ✅ 待执行 |
| 2.3 | 实现模型列表自动加载 | ✅ 待执行 |
| 2.4 | 测试 API 调用兼容性 | ✅ 待执行 |

### 阶段三：配置与集成

| 任务 | 描述 | 状态 |
|------|------|------|
| 3.1 | 创建项目级配置文件 | ✅ 待执行 |
| 3.2 | 实现环境变量支持 | ✅ 待执行 |
| 3.3 | 集成到命令行工具 | ✅ 待执行 |
| 3.4 | 添加使用文档 | ✅ 待执行 |

### 阶段四：测试与验证

| 任务 | 描述 | 状态 |
|------|------|------|
| 4.1 | 测试模型列表查询 | ✅ 待执行 |
| 4.2 | 测试基本聊天功能 | ✅ 待执行 |
| 4.3 | 测试编程辅助功能 | ✅ 待执行 |
| 4.4 | 测试多模型切换 | ✅ 待执行 |
| 4.5 | 性能与稳定性测试 | ✅ 待执行 |

## 详细技术实现

### 方案一：使用现有 OpenAI 兼容 provider

**优点**：零代码修改，配置即可使用
**缺点**：模型信息需要手动配置

#### 实现步骤

1. **创建配置文件** (`opencode.jsonc`)：

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "local-qwen": {
      "npm": "@ai-sdk/openai-compatible",
      "api": "http://192.168.102.99:8000/v1",
      "name": "Local Qwen Models",
      "env": ["LOCAL_QWEN_API_KEY"],
      "models": {
        "qwen3-max-2026-01-23": {
          "id": "qwen3-max-2026-01-23",
          "name": "Qwen3 Max 2026-01-23",
          "family": "qwen",
          "attachment": true,
          "reasoning": true,
          "tool_call": true,
          "temperature": true,
          "limit": {
            "context": 131072,
            "output": 4096
          },
          "cost": {
            "input": 0,
            "output": 0
          }
        },
        "qwen3-coder-plus": {
          "id": "qwen3-coder-plus",
          "name": "Qwen3 Coder Plus",
          "family": "qwen-coder",
          "attachment": true,
          "reasoning": true,
          "tool_call": true,
          "temperature": true,
          "limit": {
            "context": 131072,
            "output": 4096
          },
          "cost": {
            "input": 0,
            "output": 0
          }
        }
        // 其他模型配置...
      }
    }
  },
  "model": "local-qwen/qwen3-max-2026-01-23"
}
```

2. **设置环境变量**：

```bash
export LOCAL_QWEN_API_KEY="sk-token1"
```

3. **测试验证**：

```bash
# 查看可用模型
opencode models local-qwen

# 测试基本功能
echo "Hello" | opencode run --model local-qwen/qwen3-max-2026-01-23
```

### 方案二：创建专用本地模型 provider

**优点**：自动模型发现，更好的集成体验
**缺点**：需要修改源代码

#### 实现步骤

1. **创建本地模型 provider 模块**：

```typescript
// packages/opencode/src/provider/sdk/local-model/src/local-model-provider.ts
import type { LanguageModelV2 } from "@ai-sdk/provider"
import { OpenAICompatibleChatLanguageModel } from "@ai-sdk/openai-compatible"
import { type FetchFunction, withoutTrailingSlash, withUserAgentSuffix } from "@ai-sdk/provider-utils"
import { OpenAIResponsesLanguageModel } from "../../../openai-compatible/src/responses/openai-responses-language-model"

const VERSION = "1.0.0"

export type LocalModelId = string

export interface LocalModelProviderSettings {
  apiKey?: string
  baseURL?: string
  name?: string
  headers?: Record<string, string>
  fetch?: FetchFunction
}

export interface LocalModelProvider {
  (modelId: LocalModelId): LanguageModelV2
  chat(modelId: LocalModelId): LanguageModelV2
  responses(modelId: LocalModelId): LanguageModelV2
  languageModel(modelId: LocalModelId): LanguageModelV2
}

export function createLocalModel(options: LocalModelProviderSettings = {}): LocalModelProvider {
  // 处理完整的 chat/completions URL
  let baseURL = options.baseURL ?? "http://localhost:8000/v1"
  if (baseURL.includes("/chat/completions")) {
    baseURL = baseURL.replace("/chat/completions", "")
  }
  baseURL = withoutTrailingSlash(baseURL)

  if (!baseURL) {
    throw new Error("baseURL is required")
  }

  const headers = {
    ...(options.apiKey && { Authorization: `Bearer ${options.apiKey}` }),
    "Content-Type": "application/json",
    ...options.headers,
  }

  const getHeaders = () => withUserAgentSuffix(headers, `ai-sdk/local-model/${VERSION}`)

  const createChatModel = (modelId: LocalModelId) => {
    return new OpenAICompatibleChatLanguageModel(modelId, {
      provider: `${options.name ?? "local-model"}.chat`,
      headers: getHeaders,
      url: ({ path }) => `${baseURL}${path}`,
      fetch: options.fetch,
    })
  }

  const createResponsesModel = (modelId: LocalModelId) => {
    return new OpenAIResponsesLanguageModel(modelId, {
      provider: `${options.name ?? "local-model"}.responses`,
      headers: getHeaders,
      url: ({ path }) => `${baseURL}${path}`,
      fetch: options.fetch,
    })
  }

  const createLanguageModel = (modelId: LocalModelId) => createChatModel(modelId)

  const provider = function (modelId: LocalModelId) {
    return createChatModel(modelId)
  }

  provider.languageModel = createLanguageModel
  provider.chat = createChatModel
  provider.responses = createResponsesModel

  return provider as LocalModelProvider
}

export const localModel = createLocalModel()
```

2. **注册新的 provider**：

```typescript
// packages/opencode/src/provider/provider.ts
// 在导入部分添加
import { createLocalModel } from "./sdk/local-model/src/local-model-provider"

// 在 BUNDLED_PROVIDERS 中添加
const BUNDLED_PROVIDERS: Record<string, (options: any) => SDK> = {
  // ... 现有 providers ...
  "@ai-sdk/local-model": createLocalModel,
}

// 在 CUSTOM_LOADERS 中添加
const CUSTOM_LOADERS: Record<string, CustomLoader> = {
  // ... 现有 loaders ...
  async "local-model"(input) {
    return {
      autoload: true,
      options: {},
    }
  },
}
```

3. **实现模型自动发现**：

```typescript
// 自动从本地 API 获取模型列表
async function fetchLocalModels(baseURL: string, apiKey: string) {
  const response = await fetch(`${baseURL}/models`, {
    headers: {
      Authorization: `Bearer ${apiKey}`,
      "Content-Type": "application/json",
    },
  })
  
  if (!response.ok) {
    throw new Error(`Failed to fetch models: ${response.status}`)
  }
  
  const data = await response.json()
  return data.data || []
}
```

## 配置示例

### 1. 项目级配置

**文件**：`opencode.jsonc`

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "local-qwen": {
      "options": {
        "apiKey": "sk-token1",
        "baseURL": "http://192.168.102.99:8000/v1"
      }
    }
  },
  "model": "local-qwen/qwen3-max-2026-01-23",
  "small_model": "local-qwen/qwen-turbo-2025-02-11"
}
```

### 2. 用户级配置

**文件**：`~/.opencode/opencode.jsonc`

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "local-qwen": {
      "options": {
        "baseURL": "http://192.168.102.99:8000/v1"
      }
    }
  }
}
```

### 3. 环境变量配置

```bash
# .env 文件或系统环境变量
LOCAL_QWEN_API_KEY=sk-token1
LOCAL_QWEN_BASE_URL=http://192.168.102.99:8000/v1
```

## 使用指南

### 基本使用

```bash
# 启动交互式终端
opencode tui

# 运行特定任务
opencode run "请帮我重构这段代码..."

# 指定模型
opencode run --model local-qwen/qwen3-coder-plus "请帮我优化这个函数..."
```

### 高级功能

1. **模型切换**：
   ```bash
   # 切换到代码专用模型
   opencode run --model local-qwen/qwen3-coder-plus "请帮我分析这段代码的性能问题..."
   
   # 切换到视觉模型
   opencode run --model local-qwen/qwen3-vl-plus "请分析这个图片中的内容..."
   ```

2. **批量任务**：
   ```bash
   # 批量处理文件
   opencode run --model local-qwen/qwen3-coder-plus "请为这些文件添加类型注释..." --files src/**/*.ts
   ```

3. **会话管理**：
   ```bash
   # 保存会话
   opencode session save
   
   # 恢复会话
   opencode session load <session-id>
   ```

## 故障排除

### 常见问题

1. **连接失败**：
   - 检查本地大模型服务是否运行
   - 验证 baseURL 是否正确
   - 确认 API Key 是否有效

2. **模型未找到**：
   - 检查模型 ID 是否正确
   - 验证模型是否在本地服务中可用
   - 运行 `opencode models local-qwen` 查看可用模型

3. **API 错误**：
   - 检查本地大模型的 API 格式是否符合 OpenAI 标准
   - 查看本地服务的日志
   - 验证网络连接

### 调试命令

```bash
# 查看详细日志
opencode run --debug "测试消息"

# 检查配置
opencode debug config

# 测试网络连接
opencode debug network
```

## 性能优化

1. **缓存配置**：
   ```jsonc
   {
     "provider": {
       "local-qwen": {
         "options": {
           "setCacheKey": true
         }
       }
     }
   }
   ```

2. **超时设置**：
   ```jsonc
   {
     "provider": {
       "local-qwen": {
         "options": {
           "timeout": 60000 // 60秒
         }
       }
     }
   }
   ```

3. **批量请求**：
   - 合并多个小请求为一个大请求
   - 利用上下文窗口最大化单次请求的价值

## 结论

通过本计划的实施，opencode 项目将能够：

1. **无缝接入**本地部署的 Qwen 系列大模型
2. **充分利用**本地计算资源，提高响应速度
3. **保持兼容**现有功能和工作流程
4. **支持扩展**到其他本地模型服务

该方案既利用了 opencode 现有的架构优势，又提供了灵活的配置选项，是一个低风险、高收益的解决方案。

---

**计划版本**：1.0
**创建日期**：2026-01-27
**适用场景**：本地大模型集成到 opencode 开发环境
**技术栈**：TypeScript, AI SDK, OpenAI Compatible API
